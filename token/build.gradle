plugins {
    id 'common-conventions'
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.kotlin

            groupId = 'printscript'
            artifactId = 'token'
            version = '1.0.0'
        }
    }
}
