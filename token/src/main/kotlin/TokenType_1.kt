enum class TokenType_1 {
    STRING_TYPE,
    NUMBER_TYPE,
    BOOLEAN_TYPE,

    STRING,
    NUMBER,
    BOOLEAN_TRUE,
    BOOLEAN_FALSE,

    IDENTIFIER,
    SEMICOLON,

    LET_KEYWORD,
    CONST_KEYWORD,

    PRINTLN_FUNCTION,
    IF_FUNCTION,
    ELSE,
    READINPUT_FUNCTION,
    READENV_FUNCTION,

    LPAREN,
    RPAREN,
    LKEY,
    RKEY,

    EQ,
    COLON,

    PLUS,
    MINUS,
    TIMES,
    DIV,
}
