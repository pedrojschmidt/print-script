enum class TokenType {
    STRING_TYPE,
    NUMBER_TYPE,

    STRING,
    NUMBER,

    IDENTIFIER,
    SEMICOLON,

    LET_KEYWORD,

    PRINTLN_FUNCTION,
    LPAREN,
    RPAREN,
    EQ,
    COLON,

    PLUS,
    MINUS,
    TIMES,
    DIV,
}
