enum class TokenType {
    STRING_TYPE,
    NUMBER_TYPE,

    STRING,
    NUMBER,

    IDENTIFIER,
    ASSIGNATION,
    SEMICOLON,

    LET_KEYWORD,
    VAR_KEYWORD,
    CONST_KEYWORD,

    PRINTLN_FUNCTION,
    LPAREN,
    RPAREN,
    EQ,
    COLON,
    NEWLINE,
}
