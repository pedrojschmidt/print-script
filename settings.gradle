plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.5.0'
}
include ('kotlin', 'token', 'ast', 'parser', 'lexer', 'interpreter', 'formatter', 'cli')
include 'static_code_analyzer'
