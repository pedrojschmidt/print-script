plugins {
    id 'common-conventions'
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.kotlin

            groupId = 'printscritp'
            artifactId = 'ast'
            version = '1.0.0'
        }
    }
}
